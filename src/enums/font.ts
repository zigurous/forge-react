export enum FontWeight {
  thin = 100,
  extralight = 200,
  light = 300,
  regular = 400,
  medium = 500,
  semibold = 600,
  bold = 700,
  extrabold = 800,
  black = 900,
}

export enum FontSize {
  xs = 'xs',
  sm = 'sm',
  md = 'md',
  lg = 'lg',
  xl = 'xl',
}

export enum FontType {
  Display = 'display',
  Title = 'title',
  TitleLarge = 'title-lg',
  TitleMedium = 'title-md',
  TitleSmall = 'title-sm',
  Subtitle = 'subtitle',
  Body = 'body',
  BodyLarge = 'body-lg',
  BodyMedium = 'body-md',
  BodySmall = 'body-sm',
  Caption = 'caption',
  Eyebrow = 'eyebrow',
}
